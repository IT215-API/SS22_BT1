# A secret key used to sign the JWT tokens.
# IMPORTANT: Generate a strong, random string for production.
# Example: openssl rand -hex 32
SECRET_KEY="your-very-secure-and-long-secret-key-replace-me"

# The algorithm used for signing JWT tokens.
# HS256 is recommended for symmetric key signing.
ALGORITHM="HS256"